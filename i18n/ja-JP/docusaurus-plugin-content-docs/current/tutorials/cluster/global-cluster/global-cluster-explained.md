---
title: "グローバルクラスターの概要 | Cloud"
slug: /global-cluster-explained
sidebar_key: global-cluster-explained
sidebar_label: "グローバルクラスターの概要"
beta: FALSE
notebook: FALSE
description: "Zilliz Cloud のグローバルクラスターでは、同じクラウドプロバイダーの複数リージョンに、プライマリークラスターと複数の読み取り専用セカンダリークラスターをデプロイできます。"
type: origin
token: AICcwQ55yiNqEPkjdV6cb2i8nqe
sidebar_position: 1
keywords: 
  - zilliz
  - ベクトルデータベース
  - cloud
  - milvus
  - グローバルクラスター
  - switchover
  - failover
  - disaster recovery
  - high availability

---

import Admonition from '@theme/Admonition';


# グローバルクラスターの概要

Zilliz Cloud **グローバルクラスター** を使用すると、同じクラウドプロバイダー上の複数のリージョンに、プライマリークラスターと複数の読み取り専用セカンダリークラスターをデプロイできます。

この機能は、グローバルに分散したミッションクリティカルなアプリケーション向けに設計されており、リージョン障害に対する耐性と、世界中のユーザーに対する低レイテンシーのローカル読み取りを実現します。

<Admonition type="info" icon="📘" title="Notes">

この機能は、**ビジネスクリティカル** プロジェクトの **Dedicated** クラスターでのみ利用可能です。

</Admonition>

## 概要\{#overview}

Zilliz Cloud **グローバルクラスター** は、同じクラウドプロバイダー上の異なるリージョンにデプロイされた、1つの **プライマリークラスター** と最大 **5つの読み取り専用** **セカンダリークラスター** で構成されます。

- プライマリークラスター: システムの権威ある中核部分です。すべての書き込み操作を処理します。また、読み取りリクエストを処理する能力は、すべてのセカンダリークラスターと同じです。

- セカンダリークラスター: 地理的に分散されたフォロワーです。2つの重要な目的があります。ディザスタリカバリーのスタンバイとして機能することと、そのリージョンのユーザーに対してローカルの読み取り専用トラフィックを提供することです。

すべての書き込みはプライマリークラスターに向けられます。Zilliz Cloud は、プライマリークラスターからすべてのセカンダリークラスターへのデータ変更を自動的にレプリケートします。

次の図は、Zilliz Cloud でのグローバルクラスターの動作を示しています。

![UZjtwUeaxh2lDsb9eeOclNZ6nae](https://zdoc-images.s3.us-west-2.amazonaws.com/UZjtwUeaxh2lDsb9eeOclNZ6nae.png)

このマルチリージョン構成は、以下のメリットを提供します。

- **リージョン障害に対する耐性**: プライマリークラスターが障害を起こしたり停止したりした場合、セカンダリークラスターをプライマリークラスターとして昇格させることができます。

- **低レイテンシー読み取り**: データの完全なコピーが複数の地理的な場所で利用可能であるため、アプリケーションはレイテンシーを最小化するために最も近いリージョンから読み取ることができます。

## 典型的なユースケース\{#typical-use-cases}

グローバルクラスター機能には、2つの典型的なユースケースがあります。

- **ディザスタリカバリーと高可用性:** フェイルオーバーのために複数のリージョンにクラスターをデプロイします。この場合、**グローバル** **エンドポイント** — 変更されることのない単一の統一URL — を介して接続します。Zilliz Cloud は、書き込みリクエストをプライマリークラスターに、読み取りリクエストをレイテンシーに基づいて最も近いセカンダリに自動的にルーティングします。スイッチオーバーまたはフェイルオーバー中は、エンドポイントが自動的に再ルーティングされ、コードの変更は必要ありません。

- **環境間のデータレプリケーション:** 同じリージョンまたは異なるリージョンに複数のクラスター（例: 本番環境とテスト環境）を実行し、それら間でデータをレプリケートします。この場合、各クラスターの **パブリック** **エンドポイント** を使用して直接接続します。

詳細については、[グローバルクラスターへの接続](./connect-to-global-cluster) を参照してください。

## スイッチオーバーとフェイルオーバー\{#switchover-and-failover}

Zilliz Cloud グローバルクラスターは、スイッチオーバーとフェイルオーバーをサポートしています。両方の操作は、どのリージョンがプライマリークラスターをホストするかを変更し、グローバルエンドポイントは自動的に再ルーティングされます。

詳細については、[スイッチオーバーとフェイルオーバー](./switchover-and-failover) を参照してください。

## 請求\{#billing}

グローバルクラスターでは、プライマリークラスターとセカンダリークラスターの両方が、通常の Zilliz Cloud [Dedicated クラスター](./dedicated-cluster-cost) として、コンピュートとストレージの使用量に対して請求され、クラスター間のデータレプリケーションに対する追加の [データ転送](./data-transfer-cost) 料金が発生します。

グローバルクラスターの構成が以下のようであると仮定します。

- リージョンAにプライマリークラスター cluster_01

- 2つのセカンダリークラスター:

    - リージョンBに cluster_02

    - リージョンCに cluster_03

以下の合計が請求されます。

- cluster_01、cluster_02、cluster_03 の **ベクトルデータベース（コンピュート）費用**

- `cluster_01`、`cluster_02`、`cluster_03` の **ストレージ費用**

- `cluster_01` から `cluster_02` および `cluster_03` への **データ転送費用**

詳細な料金については、[Zilliz Cloud 料金表](https://zilliz.com/pricing/pricing-guide) を参照してください。

<Admonition type="info" icon="📘" title="Notes">

[フェイルオーバー](./switchover-and-failover#perform-a-failover) 後にごみ箱に入れられたクラスターは、**ストレージ** のみで請求されます。

</Admonition>

## 考慮事項\{#considerations}

- **プロジェクトプランの利用可否**: グローバルクラスター機能にアクセスするには、ビジネスクリティカルプランのマルチリージョンプロジェクトが必要です。また、グローバルクラスター内のセカンダリークラスターのリージョンは、[プロジェクト](./manage-projects) でサポートされているリージョンに限定されます。

- **アクセス制御**: グローバルクラスターを構成するには、プロジェクト管理者である必要があります。

- **クラスター構成**:

    - セカンダリークラスターは最大5つまで追加できます。

    - セカンダリークラスターは、プライマリと同じクラウドプロバイダーおよびクラスタータイプを使用する必要があります。

    - Query CU 数はプライマリによって制御されます。セカンダリは自動的に追従します。

    - レプリカ数はクラスターごとに独立して制御されます。ダイナミックスケーリングとスケジュールスケーリングもクラスターごとに独立しています。

- **クラスター操作:**

    すべてのクラスター操作がプライマリとセカンダリの両方で利用できるわけではありません。次の表は、それぞれでサポートされている内容をまとめたものです。

    <table>
       <tr>
         <th><p><strong>操作</strong></p></th>
         <th><p><strong>プライマリ</strong></p></th>
         <th><p><strong>セカンダリ</strong></p></th>
         <th><p><strong>備考</strong></p></th>
       </tr>
       <tr>
         <td><p>読み取り（search、query）</p></td>
         <td><p>Yes</p></td>
         <td><p>Yes</p></td>
         <td><p>--</p></td>
       </tr>
       <tr>
         <td><p>書き込み（insert、upsert、delete）</p></td>
         <td><p>Yes</p></td>
         <td><p>No</p></td>
         <td><p>プライマリークラスターのみが書き込み操作を受け付けます。セカンダリークラスターへの書き込みは失敗します。</p></td>
       </tr>
       <tr>
         <td><p>Query CU スケーリング</p></td>
         <td><p>Yes</p></td>
         <td><p>No</p></td>
         <td><p>Query CU の変更はプライマリに適用されます。セカンダリは自動的に追従します。</p></td>
       </tr>
       <tr>
         <td><p>レプリカスケーリング</p></td>
         <td><p>Yes</p></td>
         <td><p>Yes</p></td>
         <td><p>各クラスターは独自のレプリカ数を制御します。ダイナミックスケーリングとスケジュールスケーリングの設定も独立しています。</p></td>
       </tr>
       <tr>
         <td><p>インポート</p></td>
         <td><p>No</p></td>
         <td><p>No</p></td>
         <td><p>まもなくサポートされます。</p></td>
       </tr>
       <tr>
         <td><p>マイグレーション</p></td>
         <td><p>Yes</p></td>
         <td><p>No</p></td>
         <td><p>マイグレーションはプライマリークラスターのみでサポートされています。プライマリークラスターにマイグレートされたすべてのデータは、セカンダリークラスターにレプリケートされます。</p></td>
       </tr>
       <tr>
         <td><p>バックアップ</p></td>
         <td><p>Yes</p></td>
         <td><p>No</p></td>
         <td><p>バックアップはプライマリークラスターのみで作成できます。</p><p>自動バックアップポリシーもプライマリでのみ実行されます。</p></td>
       </tr>
       <tr>
         <td><p>リストア</p></td>
         <td><p>No</p></td>
         <td><p>No</p></td>
         <td><p>まもなくサポートされます。</p></td>
       </tr>
       <tr>
         <td><p>一時停止 / 再開</p></td>
         <td><p>No</p></td>
         <td><p>No</p></td>
         <td><p>すべてのプライマリおよびセカンダリークラスターは一時停止できません。</p></td>
       </tr>
       <tr>
         <td><p>スイッチオーバー</p></td>
         <td><p>Yes</p></td>
         <td><p>—</p></td>
         <td><p>すべてのプライマリおよびセカンダリークラスターが RUNNING の場合にのみトリガーできます。</p></td>
       </tr>
       <tr>
         <td><p>フェイルオーバー</p></td>
         <td><p>Yes</p></td>
         <td><p>—</p></td>
         <td><p>いつでもトリガーできます。これは高リスクの緊急操作です。</p></td>
       </tr>
    </table>

- **サポートされていない機能**

    - プライベートグローバルエンドポイントの設定はサポートされていません。グローバルエンドポイントにはパブリックインターネットアクセスが必要です。

    - カスタマーマネージド暗号化キー（[CMEK](./cmek)）は、グローバルクラスターではサポートされていません。CMEK が有効になっているクラスターは、グローバルクラスターに変換できません。

